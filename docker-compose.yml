version: '3.8'

services:
  # Backend API (using existing AWS RDS database)
  backend:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: marketplace-backend
    restart: unless-stopped
    environment:
      DB_HOST: market-db.cv0q0cewwvb9.us-west-1.rds.amazonaws.com
      DB_PORT: 5432
      DB_NAME: postgres
      DB_USER: folders
      DB_PASSWORD: tO8uOtRGjBVvjISyHJFp
      JWT_SECRET_KEY: fab197034fe72822c3c8288425ef7d51a63cbe2091155391830f9d4cbe94ee17
      JWT_ALGORITHM: HS256
      SMTP_HOST: smtp.gmail.com
      SMTP_PORT: 587
      SMTP_USER: us.unimarket@gmail.com
      SMTP_PASSWORD: pkzfdiztswrpmqxn
      FROM_EMAIL: us.unimarket@gmail.com
    ports:
      - "8000:8000"
    volumes:
      - ./api:/app
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/docs"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Frontend
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: marketplace-frontend
    restart: unless-stopped
    environment:
      VITE_API_URL: http://localhost:8000/
    ports:
      - "3000:80"
    depends_on:
      - backend