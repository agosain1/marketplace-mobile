version: '3.8'

services:
  # Backend API with hot reload (using existing AWS RDS database)
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: marketplace-backend-dev
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: ["uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]